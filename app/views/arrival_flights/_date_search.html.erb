<%= form_tag(arrival_flights_path, :method => "get", :class =>"pax-action") do %>
  <%= label_tag(:q, "Date(DD/MM/YYYY)") %>
  <%= text_field_tag(:date, params[:date], :size => 10, :maxlength => 10) %>
  <%= submit_tag("Search") %>
<% end %>