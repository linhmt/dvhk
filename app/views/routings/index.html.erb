<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.all", :default => "Summary")}", "#" %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_routing_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <div class="inner">
      <table class="table" border="2">
        <tr>             
          <th width="30%">
            <%= t("activerecord.attributes.routing.routing", :default => "Routing") %>
          </th>
          <th width="15%">
            <%= t("web-app-theme.created_at", :default => "Waiting No.")  %>
          </th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @routings.each do |routing| -%>
          <tr class="<%= cycle("odd", "even") %>">
            <td><%=link_to t("activerecord.attributes.routing.routing", :default => "#{h(routing.destination)}"), 
                  routing_passengers_path(routing)-%></td>
            <td>Size</td> 
            <td class="last">
              <%= link_to "#{t("web-app-theme.show", :default => "New Passenger")}", new_routing_passenger_path(routing) -%>
              Accepted List
            </td>
          </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>

      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
